services:
  mysql:
    image: mysql:8.0
    container_name: coupon-mysql
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "1234"
      MYSQL_DATABASE: coupon
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
