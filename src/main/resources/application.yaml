spring:
  application:
    name: coupon
  datasource:
    url: jdbc:mysql://localhost:3307/coupon
    username: root
    password: "1234"
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true